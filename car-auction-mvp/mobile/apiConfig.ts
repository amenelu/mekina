/**
 * IMPORTANT: Replace this with your computer's current local IP address.
 * On Windows, run `ipconfig` in the command prompt. On macOS/Linux, run `ifconfig` or `ip addr`.
 */
export const API_BASE_URL = "http://192.168.100.9:5001";
